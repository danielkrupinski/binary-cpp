#ifndef BINARY_H
#define BINARY_H

#include <iostream>
#include <cmath>

namespace Binary {
    void ask();
    void process(int);
    long long int to_binary(int);
    int to_decimal(long long int);
}

#endif
